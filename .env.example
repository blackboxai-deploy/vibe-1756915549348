# 🔧 HYPER-VIDEO SAAS - ENVIRONMENT VARIABLES
# Copy this file to .env.local and fill with your actual values

# =============================================================================
# CORE APPLICATION SETTINGS
# =============================================================================

# Your application URL (change in production)
NEXT_PUBLIC_APP_URL="http://localhost:3000"

# Admin email for accessing admin dashboard
# Only this email can access /admin/dashboard
ADMIN_EMAIL="admin@yourdomain.com"

# =============================================================================
# DATABASE CONFIGURATION
# =============================================================================

# PostgreSQL database connection string
# For local development, install PostgreSQL and create database
# For production, use managed database (Supabase, PlanetScale, etc.)
DATABASE_URL="postgresql://username:password@localhost:5432/hypervideo_db"

# Example for Supabase:
# DATABASE_URL="postgresql://postgres:[password]@db.[project].supabase.co:5432/postgres"

# =============================================================================
# AUTHENTICATION (OPTIONAL - CLERK)
# =============================================================================

# Get these from https://dashboard.clerk.com
# Create new application and copy the keys
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY="pk_test_your_publishable_key_here"
CLERK_SECRET_KEY="sk_test_your_secret_key_here"

# Clerk redirect URLs
NEXT_PUBLIC_CLERK_SIGN_IN_URL="/sign-in"
NEXT_PUBLIC_CLERK_SIGN_UP_URL="/sign-up"
NEXT_PUBLIC_CLERK_AFTER_SIGN_IN_URL="/dashboard"
NEXT_PUBLIC_CLERK_AFTER_SIGN_UP_URL="/dashboard"

# =============================================================================
# AI SERVICE CONFIGURATION  
# =============================================================================

# For Claude AI (Prompt Studio) - ALREADY CONFIGURED
# Uses custom endpoint, no API key needed
OPENROUTER_API_KEY="configured_automatically"

# =============================================================================
# USER API KEYS (FOR DEMO/TESTING ONLY)
# =============================================================================

# Fallback Replicate API key for testing Video Generator
# In production, users must provide their own API keys
# Get your key from: https://replicate.com/account/api-tokens
REPLICATE_API_KEY_FALLBACK="r_your_replicate_api_key_here"

# =============================================================================
# PAYMENT CONFIGURATION (MANUAL SYSTEM)
# =============================================================================

# Bank account details for manual transfer (update with real info)
BANK_NAME="Bank BCA"
BANK_ACCOUNT_NUMBER="1234567890"
BANK_ACCOUNT_NAME="PT Your Company Name"

# =============================================================================
# SECURITY & ENCRYPTION
# =============================================================================

# Secret for encrypting user API keys in database
# Generate with: openssl rand -base64 32
ENCRYPTION_KEY="your_32_character_encryption_key_here"

# NextAuth secret (if using NextAuth instead of Clerk)
NEXTAUTH_SECRET="your_nextauth_secret_here"
NEXTAUTH_URL="http://localhost:3000"

# =============================================================================
# OPTIONAL INTEGRATIONS
# =============================================================================

# Google Analytics (for tracking)
NEXT_PUBLIC_GA_ID="G-XXXXXXXXXX"

# Sentry (for error tracking)
SENTRY_DSN="https://your-sentry-dsn-here"

# =============================================================================
# PRODUCTION NOTES
# =============================================================================

# For production deployment:
# 1. Update NEXT_PUBLIC_APP_URL to your domain
# 2. Use strong DATABASE_URL from cloud provider
# 3. Set secure ADMIN_EMAIL
# 4. Generate strong ENCRYPTION_KEY
# 5. Configure real Clerk keys (if using auth)
# 6. Remove REPLICATE_API_KEY_FALLBACK (users must provide own)

# For Vercel deployment:
# Add all these variables in Vercel Dashboard → Settings → Environment Variables